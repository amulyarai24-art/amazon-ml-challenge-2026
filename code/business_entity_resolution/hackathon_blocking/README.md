# Entity Matching - Blocking Pipeline (Person 2)

High-recall candidate pair generation for Entity Matching using Prefix Indexing and TF-IDF Character N-Gram K-NN.

## Quick Setup

1. Open project directory in VS Code Terminal:
```bash
python -m venv venv
